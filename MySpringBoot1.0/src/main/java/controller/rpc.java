package controller;

import org.apache.catalina.core.ApplicationContext;
import org.apache.log4j.Logger;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;


//@Controller
public class rpc {

	/*@Autowired
	private IDictionaryConfigApiService dictionaryConfigApiService;

	private static Logger log = Logger.getLogger(rpc.class);

	public void RPC() {

		long old = System.currentTimeMillis();
		dictionaryConfigApiService.findAllDictConfigValues("shanxisheng", "product_service_fee_config", "88");
		long ne = System.currentTimeMillis();
		log.info(ne - old);

	}*/
}
